[Service]
# /run is recreated every boot and the stock VPP package does not create the
# log directory used by Ly Route's startup configuration.
ExecStartPre=
ExecStartPre=/usr/bin/install -d -m 0755 -o root -g vpp /run/vpp /var/log/vpp
