[Service]
# VPP's Restart=always restarts the process without reactivating dependent
# oneshot units. Enable the session layer after every process start so VCL
# DNS applications can always register after a VPP crash or runtime restart.
ExecStartPost=/usr/lib/ly-route/enable-vpp-session.sh
